# Rust
/target
**/*.rs.bk
*.pdb

# GUI / Node
gui/node_modules
gui/dist
gui/src-tauri/target
gui/.vite

# Admin console (legacy/gui-admin/) — same build artifacts as gui/
legacy/gui-admin/node_modules
legacy/gui-admin/dist
legacy/gui-admin/src-tauri/target
legacy/gui-admin/.vite

# Local config, keys, state — NEVER commit secrets or node identity
*.key
*.pem
/state
*.local.toml
.env
.env.*

# Claude Code local overrides
.claude/settings.local.json

# Course submission forms + reports (personal info / not source — keep out of repo)
*.docx
~$*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
/legacy/dist-linux
ssh-key-2026-06-13.key*
gui/src-tauri/resources/meshd
