{
  "$schema": "https://schema.tauri.app/config/1",
  "tauri": {
    "bundle": {
      "comment": "No bundled daemon on Windows yet — meshd's Windows port (named pipe + Wintun) is WIP, so don't reference a resource that isn't built.",
      "resources": []
    }
  }
}
